# 📝 Пошаговая инструкция: Как выложить проект на GitHub и опубликовать на GitHub Pages

## Часть 1: Выкладываем проект на GitHub

### Шаг 1.1: Откройте терминал
Откройте терминал на вашем Mac (нажмите `Cmd + Пробел`, введите "Терминал" и нажмите Enter).

### Шаг 1.2: Перейдите в папку проекта
В терминале введите:
```bash
cd /Users/arisha/Desktop/diplom
```

### Шаг 1.3: Инициализируйте Git репозиторий
Введите команду:
```bash
git init
```
Эта команда создаст скрытую папку `.git` в вашем проекте.

### Шаг 1.4: Добавьте все файлы проекта
Введите команду:
```bash
git add .
```
Эта команда добавит все файлы проекта в очередь для загрузки (кроме тех, что в `.gitignore`).

### Шаг 1.5: Создайте первый коммит (сохранение)
Введите команду:
```bash
git commit -m "Первая версия проекта"
```
Эта команда сохранит все файлы в истории Git.

### Шаг 1.6: Создайте репозиторий на GitHub
1. Откройте браузер и зайдите на [github.com](https://github.com)
2. Войдите в свой аккаунт (или создайте новый, если его нет)
3. Нажмите на зеленую кнопку **"New"** или **"+"** в правом верхнем углу
4. Выберите **"New repository"**
5. Заполните форму:
   - **Repository name**: `Diplom` (или другое название)
   - **Description**: "Система управления кинотеатром" (необязательно)
   - Выберите **Public** (публичный репозиторий)
   - **НЕ** ставьте галочки на "Add a README file", "Add .gitignore", "Choose a license" (у вас уже есть эти файлы)
6. Нажмите зеленую кнопку **"Create repository"**

### Шаг 1.7: Подключите ваш проект к GitHub репозиторию
После создания репозитория GitHub покажет вам инструкции. Вам нужна секция **"...or push an existing repository from the command line"**.

В терминале выполните команды (замените `ВАШ_НИКНЕЙМ` на ваш никнейм на GitHub):

```bash
git branch -M main
git remote add origin https://github.com/ВАШ_НИКНЕЙМ/Diplom.git
git push -u origin main
```

**Пример:** Если ваш никнейм `pavelb-sme`, то команда будет:
```bash
git remote add origin https://github.com/pavelb-sme/Diplom.git
```

При первом `push` GitHub попросит вас ввести логин и пароль (или токен доступа).

---

## Часть 2: Публикуем проект на GitHub Pages

### Шаг 2.1: Соберите проект для публикации
В терминале (все еще в папке проекта) введите:
```bash
npm run build
```
Эта команда создаст папку `dist` с готовым для публикации проектом.

### Шаг 2.2: Опубликуйте на GitHub Pages
Введите команду:
```bash
npm run deploy
```
Эта команда:
- Создаст специальную ветку `gh-pages` в вашем репозитории
- Загрузит туда собранный проект
- Активирует GitHub Pages

### Шаг 2.3: Подождите несколько минут
GitHub Pages обычно активируется в течение 1-5 минут.

### Шаг 2.4: Проверьте, что сайт работает
Откройте в браузере:
```
https://ВАШ_НИКНЕЙМ.github.io/Diplom/
```

**Пример:** Если ваш никнейм `pavelb-sme`, то адрес будет:
```
https://pavelb-sme.github.io/Diplom/
```

---

## ✅ Готово!

Теперь ваш проект:
- ✅ Выложен на GitHub
- ✅ Опубликован на GitHub Pages
- ✅ Доступен по ссылке из README.md

---

## 🔄 Если нужно обновить проект позже

Когда вы внесете изменения в проект:

1. **Добавьте изменения:**
   ```bash
   git add .
   ```

2. **Сохраните изменения:**
   ```bash
   git commit -m "Описание изменений"
   ```

3. **Загрузите на GitHub:**
   ```bash
   git push
   ```

4. **Обновите сайт на GitHub Pages:**
   ```bash
   npm run build
   npm run deploy
   ```

---

## ❓ Частые проблемы и решения

### Проблема: "Permission denied" при push
**Решение:** Нужно настроить доступ к GitHub. Можно использовать:
- Personal Access Token (токен доступа) вместо пароля
- SSH ключи

### Проблема: "npm run deploy" не работает
**Решение:** Убедитесь, что:
- Вы выполнили `npm run build` перед `npm run deploy`
- У вас установлен пакет `gh-pages` (должен быть в `package.json`)

### Проблема: Сайт не открывается
**Решение:**
- Подождите 5-10 минут (GitHub Pages может активироваться с задержкой)
- Проверьте настройки репозитория: Settings → Pages → должен быть выбран источник "gh-pages branch"


