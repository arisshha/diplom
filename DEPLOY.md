# Инструкция по развертыванию на Vercel

## ✅ Проверка готовности проекта

Проект готов к деплою:
- ✅ Build проходит без ошибок
- ✅ TypeScript ошибки исправлены
- ✅ Конфигурация для Vercel создана (`vercel.json`)
- ✅ База данных: используется localStorage (не требует настройки)
- ✅ API: подключен к `https://shfe-diplom.neto-server.ru`

## 🚀 Шаги для развертывания на Vercel

### Шаг 1: Подготовка GitHub репозитория

1. Убедитесь, что проект загружен в GitHub:
   ```bash
   git init
   git add .
   git commit -m "Prepare for Vercel deployment"
   git branch -M main
   git remote add origin https://github.com/ВАШ_USERNAME/ВАШ_РЕПОЗИТОРИЙ.git
   git push -u origin main
   ```

### Шаг 2: Создание аккаунта на Vercel

1. Перейдите на [vercel.com](https://vercel.com)
2. Нажмите **"Sign Up"**
3. Выберите **"Continue with GitHub"** (рекомендуется)
4. Авторизуйтесь через GitHub
5. Подтвердите доступ Vercel к вашим репозиториям

### Шаг 3: Подключение проекта к Vercel

1. В Dashboard Vercel нажмите **"Add New..."** → **"Project"**
2. Найдите ваш репозиторий в списке (или используйте поиск)
3. Нажмите **"Import"** рядом с нужным репозиторием

### Шаг 4: Настройка проекта

Vercel автоматически определит настройки из `vercel.json`, но проверьте:

**Framework Preset:** Vite (должно определиться автоматически)

**Build Settings:**
- **Build Command:** `npm run build` (уже настроено)
- **Output Directory:** `dist` (уже настроено)
- **Install Command:** `npm install` (уже настроено)

**Root Directory:** оставьте пустым (если проект в корне)

### Шаг 5: Переменные окружения (опционально)

Если нужно изменить API URL, добавьте переменную окружения:

1. В настройках проекта перейдите в **"Environment Variables"**
2. Добавьте переменную:
   - **Name:** `VITE_API_PREFIX`
   - **Value:** `https://shfe-diplom.neto-server.ru`
   - **Environment:** Production, Preview, Development

**Примечание:** Сейчас API URL захардкожен в коде, но можно сделать через переменные окружения.

### Шаг 6: Деплой

1. Нажмите **"Deploy"**
2. Дождитесь завершения сборки (обычно 1-3 минуты)
3. После успешного деплоя вы получите ссылку вида:
   ```
   https://ваш-проект.vercel.app
   ```

### Шаг 7: Проверка работы

1. Откройте ссылку в браузере
2. Проверьте:
   - ✅ Главная страница загружается
   - ✅ Кнопки "войти" и "регистрация" работают
   - ✅ Форма регистрации работает
   - ✅ Форма авторизации работает
   - ✅ Админ-панель доступна после авторизации

## 🔄 Автоматический деплой

После первого деплоя Vercel автоматически будет деплоить проект при каждом push в основную ветку:

1. Сделайте изменения в коде
2. Закоммитьте и запушьте в GitHub:
   ```bash
   git add .
   git commit -m "Your changes"
   git push
   ```
3. Vercel автоматически запустит новый деплой
4. Через несколько минут изменения будут доступны на сайте

## 📝 Дополнительные настройки

### Кастомный домен

1. В настройках проекта перейдите в **"Domains"**
2. Добавьте ваш домен
3. Следуйте инструкциям для настройки DNS

### Preview Deployments

Каждый Pull Request автоматически получает свою preview ссылку для тестирования.

### Environment Variables для разных окружений

- **Production:** для основного сайта
- **Preview:** для preview деплоев (PR)
- **Development:** для локальной разработки

## ⚠️ Важные замечания

1. **База данных:** Пользователи хранятся в localStorage браузера. Это означает:
   - Данные не синхронизируются между устройствами
   - При очистке кеша данные теряются
   - Для продакшена рекомендуется использовать реальную БД

2. **API:** Приложение использует внешний API `https://shfe-diplom.neto-server.ru`
   - Убедитесь, что API доступен из интернета
   - Проверьте CORS настройки на сервере

3. **Basename:** Проект автоматически определяет окружение:
   - На Vercel: `basename = '/'`
   - На GitHub Pages: `basename = '/Diplom'`

## 🐛 Решение проблем

### Build падает с ошибками

1. Проверьте логи в Vercel Dashboard
2. Убедитесь, что `npm run build` проходит локально
3. Проверьте версии Node.js (Vercel использует последнюю LTS)

### Сайт не открывается (404)

1. Проверьте `vercel.json` - должен быть rewrite на `/index.html`
2. Убедитесь, что `outputDirectory` указан правильно (`dist`)

### API запросы не работают

1. Проверьте CORS настройки на сервере API
2. Убедитесь, что API доступен из интернета
3. Проверьте консоль браузера на ошибки

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи в Vercel Dashboard
2. Проверьте консоль браузера (F12)
3. Убедитесь, что все зависимости установлены

---

**Готово!** Ваше приложение теперь доступно по ссылке Vercel и работает на любом компьютере! 🎉

